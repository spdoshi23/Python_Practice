# MODULES
# Module is like a code library which can be used to borrow code written by somebody else in our python program.
# 1- Built in Modules - These modules are ready to import and use and ships with the python interpreter. 
#                       there is no need to install such modules explicitly.
# 2- External Modules - These modules are imported from a third party file or can be installed using a package manager like pip or conda. 
#                       Since this code is written by someone else, we can install different versions of a same module with time.

# PIP 
# It can be used as a package manager pip to install a python module. Lets install a module called pandas using the following command
# eg--pip install pandas
# pip brings whatever we want into python interpreter, bringing it from internet.
# eg-- pip install scikit-learn

# install hashlib   (cant happen because it is a built in module)
# install tensorflow   (it will install bcz its external module)

print("hello world", 7)   #a comma btw content in "__" and content outside it brings output as actual space btw them
